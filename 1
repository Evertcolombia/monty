#include "monty.h"

void get_func(char *s)
{
	int i = 0;

	instruction_f = funcs [] = {
		{"push", f_push}
	};

	if (s == NULL)
	{
		print("Null, exit\n");
		exit(0);
	}

	while (i <= 1)
	{
		if (strcmp(funcs[i].str, s) == 0)
		{
			funcs[i].f(&stack, 0);
			break;
		} i++;
	}
}
